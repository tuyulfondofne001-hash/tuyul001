export const metadata = {
  title: "PDF Resume to XML Extractor",
  description: "Extract resume data from PDF text using OpenRouter AI",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
